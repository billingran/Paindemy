<!-- /////////////////////////////////////////////// -->
<!-- carousel all instructors -->

<section class="section_carousels">
  <div
    id="carouselExampleSlidesOnly"
    class="carousel slide"
    data-bs-ride="carousel"
  >
    <div class="carousel-inner ctn_mid_carousels">
      <div class="carousel-item active ctn_carousels">
        <div
          class="carousels"
          style="background-color: <%= allinstructors[0].themeColorUser %>"
        >
          <div class="filter_instructors"></div>
          <div class="left_instructors">
            <h2>“ <%= getBackUrl(allinstructors[0].fathUser) %> ”</h2>
            <a
              href="course/instructor/<%= urlParsed(allinstructors[0].firstnameUser) %>"
              class="central"
            >
              -- <%= getBackUrl(allinstructors[0].firstnameUser) %> <%=
              getBackUrl(allinstructors[0].lastnameUser) %>
              <iconify-icon
                icon="mdi:more-circle"
                class="icon_carousels"
              ></iconify-icon>
            </a>
          </div>
          <div class="right_instructors">
            <img
              src="/uploads/<%= allinstructors[0].imageUser[1] %>.png"
              class="d-block"
              alt="<%= allinstructors[0].getFirstnameUser() %>"
            />
          </div>
        </div>
      </div>

      <% if(typeof allinstructors !== 'undefined' && allinstructors != null &&
      allinstructors.length > 0) { %> <%
      allinstructors.slice(1).forEach(instructor => { %>
      <div class="carousel-item ctn_carousel">
        <div
          class="carousels"
          style="background-color: <%= instructor.themeColorUser %>"
        >
          <div class="filter_instructors"></div>

          <div class="left_instructors">
            <h2>“ <%= getBackUrl(instructor.fathUser) %> ”</h2>
            <a
              href="course/instructor/<%= urlParsed(instructor.firstnameUser) %>"
              class="central"
            >
              -- <%= getBackUrl(instructor.firstnameUser) %> <%=
              getBackUrl(instructor.lastnameUser) %>
              <iconify-icon
                icon="mdi:more-circle"
                class="icon_carousels"
              ></iconify-icon>
            </a>
          </div>
          <div class="right_instructors">
            <img
              src="/uploads/<%= instructor.imageUser[1] %>.png"
              class="d-block"
              alt="<%= instructor.getFirstnameUser() %>"
            />
          </div>
        </div>
      </div>
      <% }) %> <% } %>
    </div>
  </div>
</section>

<!-- /////////////////////////////////////////////// -->
<!-- All instructors -->

<section class="section_display section_mid">
  <div class="ctn_display">
    <div class="ctn_title_display">
      <div class="centralA">
        <iconify-icon
          icon="tabler:chef-hat"
          class="icon_title_display"
        ></iconify-icon>
        <h2 class="title_display">Instructors</h2>
      </div>
      <div class="mid_display">
        <a href="#" class="ctn_viceTitle_display"
          >Show ramdon
          <iconify-icon
            icon="fa:random"
            class="icon_viceTitle_display"
          ></iconify-icon>
        </a>
      </div>
    </div>
    <div>
      <ul class="ctn_images_instructors">
        <% if(typeof allinstructors !== 'undefined' && allinstructors != null &&
        allinstructors.length > 0) { %> <% allinstructors.forEach(instructor =>
        { %>
        <li class="mid_image">
          <a href="#">
            <div class="ctn_image ctn_image_instructors">
              <div
                loading="lazy"
                class="image image_instructors"
                style="
                  background-image: url('/uploads/<%= instructor.imageUser[0] %>.png');
                "
              ></div>
              <h3 class="title_instructors">
                <%= instructor.getFirstnameUser() %>
              </h3>
            </div>
          </a>
        </li>
        <% }) %> <% } else { %>
        <p>Sorry, we currently don't have instuctor</p>
        <% } %>
      </ul>
    </div>
  </div>
</section>
